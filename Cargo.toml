[workspace]
members = ["cores/chip8"]
resolver = "2"

[package]
name = "emu-ops"
version = "0.1.0"
edition = "2021"

[dependencies]
minifb = "0.25"
rand = "0.8"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = "0.4"

# Add these for features.rs
anyhow = "1.0"
sha2 = "0.10"
hex = "0.4"
log = "0.4"
